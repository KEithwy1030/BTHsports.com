# ============================================
# 环境变量配置说明
# ============================================
# 本地开发：复制此文件为 env.dev 并修改
# Zeabur 部署：在 Zeabur 后台直接设置环境变量，无需创建文件

# ============================================
# 数据库配置（Zeabur 会自动注入）
# ============================================
# 在 Zeabur 上创建 MySQL 服务后，会自动注入以下变量：
# - DB_HOST: 数据库主机地址（Zeabur 自动注入）
# - DB_PORT: 数据库端口（通常为 3306，Zeabur 自动注入）
# - DB_USER: 数据库用户名（Zeabur 自动注入）
# - DB_PASSWORD: 数据库密码（Zeabur 自动注入）
# - DB_NAME: 数据库名称（Zeabur 自动注入）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=live_sports

# ============================================
# Redis配置（可选，当前已禁用）
# ============================================
# 如果使用 Redis，Zeabur 会自动注入以下变量：
# - REDIS_HOST: Redis 主机地址
# - REDIS_PORT: Redis 端口（通常为 6379）
# - REDIS_PASSWORD: Redis 密码（如果有）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ============================================
# 服务器配置
# ============================================
# PORT: Zeabur 会自动注入，无需手动设置
# NODE_ENV: Zeabur 会自动设置为 production
PORT=7001
NODE_ENV=development

# ============================================
# CORS 配置（Zeabur 部署建议）
# ============================================
# 生产环境默认允许所有来源（*）
# 如需限制，设置为具体域名，多个用逗号分隔
# 示例：CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com
# CORS_ORIGINS=*

# ============================================
# 爬虫配置
# ============================================
CRAWLER_INTERVAL=300000
CRAWLER_DELAY=2000
CRAWLER_RETRY=3

# ============================================
# 浏览器自动化配置（Zeabur 部署建议禁用）
# ============================================
# ENABLE_BROWSER: 是否启用浏览器自动化功能
# - 云环境（Zeabur）默认禁用（容器中没有 Chrome/Chromium）
# - 本地开发环境可以设置为 true（需要安装 Chrome）
# - 设置为 true 时，爬虫会尝试使用浏览器自动化获取更准确的信号源
# ENABLE_BROWSER=false

# ============================================
# 环境文件路径（可选）
# ============================================
# 如果使用自定义环境文件路径，设置此变量
# ENV_FILE=.env.production
